{
  "name": "voxscript",
  "version": "1.2.1",
  "description": "음성 자동 번역 파이프라인 + Electron 데스크탑 앱",
  "license": "UNLICENSED",
  "main": "electron/main.js",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix ../voxscript_frontend\" \"wait-on http://localhost:5173 && electron .\"",
    "build": "npm run build --prefix ../voxscript_frontend && xcopy /E /I /Y ..\\voxscript_frontend\\dist frontend\\dist",
    "build:backend": "cd backend && pyinstaller voxscript-backend.spec --distpath ../backend-dist --workpath ../backend-build --noconfirm",
    "package": "npm run build:backend && npm run build && electron-builder",
    "electron": "electron ."
  },
  "build": {
    "appId": "com.terryblackhoodwoo.voxscript",
    "productName": "VOXScript",
    "directories": {
      "output": "dist"
    },
    "files": [
      "electron/**/*",
      "frontend/dist/**/*"
    ],
    "win": {
      "target": "nsis",
      "icon": "electron/assets/icon.ico"
    },
    "mac": {
      "target": "dmg",
      "icon": "electron/assets/icon.icns"
    },
    "nsis": {
      "oneClick": false,
      "perMachine": false,
      "allowToChangeInstallationDirectory": true
    },
    "extraResources": [
      {
        "from": "backend-dist/voxscript-backend",
        "to": "backend"
      },
      {
        "from": "resources/bin",
        "to": "bin"
      }
    ]
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "electron": "^32.0.0",
    "electron-builder": "^24.0.0",
    "wait-on": "^7.2.0"
  }
}